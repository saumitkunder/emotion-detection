name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout code
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 2: Set up Python environment
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9

    # Step 3: Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r app/requirements.txt
        pip install pytest  # Explicitly ensure pytest is installed

    # Step 4: Run unit tests
    - name: Run unit tests
      run: |
        pytest app/tests

  docker-build-push:
    runs-on: ubuntu-latest
    needs: build-test

    steps:
    # Step 5: Checkout code
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 6: Log in to Docker Hub
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Step 7: Build and push Docker image
    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/emotion-detection-app:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/emotion-detection-app:latest

  deploy-azure:
    runs-on: ubuntu-latest
    needs: docker-build-push

    steps:
    # Step 8: Authenticate with Azure
    - name: Authenticate with Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # Step 9: Deploy to Azure App Service
    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v2
      with:
        app-name: emotion-detection-app
        slot-name: production
        images: ${{ secrets.DOCKER_USERNAME }}/emotion-detection-app:latest
